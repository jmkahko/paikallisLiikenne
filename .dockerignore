# Älä kopioi näitä build-kontekstiin → nopeampi build, ei vuotoja imageen.
node_modules
dist
dist-ssr

# Salaisuudet: avain välitetään build-args-kautta, ei imageen.
.env
.env.local
.env.*.local

# Palvelimen oma config.php ei kuulu imageen — avain syötetään env-muuttujana
public/api/config.php

# Versionhallinta ja editori
.git
.gitignore
.vscode
.idea
.DS_Store

# Logit ja temp-tiedostot
*.log
err.log

# Dokumentaatio (ei tarvita imagessa)
README.md
CLAUDE.md

# Docker-tiedostot itse (eivät tarvita kontekstissa)
Dockerfile
docker-compose.yml
.dockerignore
